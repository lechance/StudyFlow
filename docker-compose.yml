# StudyFlow - Docker Compose
services:
  app:
    build: .
    container_name: studyflow-app
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    volumes:
      - studyflow_data:/app/data
    restart: unless-stopped

volumes:
  studyflow_data:
